# ── ENVIRONMENT CONFIGURATIONS ──

# Application mode (development / production)
NODE_ENV=development

# Exposed port for the local dev container
PORT=5080

# ── VITE FRONTEND CONFIGURATIONS (Pre-processed by Vite compiler) ──
# Mock API entry point prefix (if integrated in future stages)
VITE_API_URL=http://localhost:8080/api
